# Core app
streamlit>=1.35.0

# Data handling
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=14.0.0
requests>=2.31.0

# Market data
yfinance>=0.2.40

# Risk and quant
scipy>=1.11.0
arch>=6.3.0
statsmodels>=0.14.0
PyPortfolioOpt>=1.5.5
scikit-learn>=1.3.0

# Visualization
plotly>=5.18.0

# AI agents
# openai>=2.8.0 forces pip to install openai 2.x (crewai requires >=1.13.3, no upper bound).
# litellm and groq must be explicit because crewai's transitive resolution lands on openai 1.x
# which breaks litellm's import. Forcing openai 2.x + explicit litellm/groq fixes this on
# Streamlit Cloud where the two-step install.bat workaround cannot run.
crewai==1.9.3
openai>=2.8.0
litellm>=1.53.0
groq>=0.9.0

# Config and env
python-dotenv>=1.0.0
pyyaml>=6.0.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
